main
    double base
    string color2
    string color1

    base = 300.0

    for i from 0 to 5:
        drawCircle(200.0 , 50.0 * i, 50.0, 2, '', 'green')
        #drawCircle(x,y,radio, grosor, relleno, contorno)
        drawLine(base * i - 2, 200.0, 50.1 * i, 600.0, i, 'yellow')
    end
endmain