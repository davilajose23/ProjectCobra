func int fibo(int x)
    int ret
    if x < 2:
        ret = x
    else:
        ret = fibo(x - 1) + fibo(x - 2)

    end
    return ret
end

main
    int sol
    sol = 6
    sol = fibo(sol)
    print sol
end_main