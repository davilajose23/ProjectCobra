int x

func void hola()
	x += 1
end

func int test(int x, int y)
	return x + y
end

main
	bool a
	int b

	a = (1 + 2) * 3 / 3 >= 3 and 4 > 3

	if a:
		b = 1 + 2
	else:
		b = 2 + 3
	end

	if b < 50:
		b = 3
		if b < 3:
			b = 2
		end
	end

	b = test(1 + b, 2)
	
	while b < 5:
		b += 1
		print b
	end
end_main
