func int fibo(int x):
    int ret
    if x < 2:
        ret = x
    else:
        ret = fibo(x - 1) + fibo(x - 2)

    end
    return ret
end

main
    int sol
    sol = 8
    sol = fibo(sol)
    print sol
endmain